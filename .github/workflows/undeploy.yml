name: Undeploy from OpenShift

on:
  workflow_run:
    # Automatically undeploy after successful deployment
    workflows: ["Deploy to OpenShift"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      namespace:
        description: 'Target namespace for undeployment'
        required: false
        default: 'test-workflow-deploy'
        type: string
      confirm_uninstall:
        description: 'I understand this will permanently delete the deployment'
        required: true
        default: false
        type: boolean
      delay_minutes:
        description: 'Minutes to wait before auto-uninstall (default: 10)'
        required: false
        default: '10'
        type: string

env:
  NAMESPACE: ${{ github.event.inputs.namespace || 'test-workflow-deploy' }}
  DELAY_MINUTES: ${{ github.event.inputs.delay_minutes || '10' }}

jobs:
  check-confirmation:
    runs-on: ubuntu-latest
    # Only run for manual triggers without confirmation
    if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.confirm_uninstall != 'true' }}
    steps:
      - name: Log confirmation error
        run: |
          echo "❌ Manual undeploy workflow triggered without confirmation"
          echo ""
          echo "🛡️  Safety Check Failed:"
          echo "   - Manual undeployment requires explicit confirmation"
          echo "   - Please check the confirmation checkbox to proceed"
          echo "   - This prevents accidental deletion of deployments"
          echo ""
          echo "📋 To run this workflow:"
          echo "   1. Go to Actions → Undeploy from OpenShift"
          echo "   2. Click 'Run workflow'"
          echo "   3. ✅ Check the confirmation checkbox"
          echo "   4. Specify the target namespace"
          echo "   5. Click 'Run workflow'"
          echo ""
          echo "🔍 Current inputs:"
          echo "   - Namespace: ${{ github.event.inputs.namespace || 'test-workflow-deploy' }}"
          echo "   - Confirmation: ${{ github.event.inputs.confirm_uninstall }}"
          echo "   - Delay: ${{ github.event.inputs.delay_minutes || '10' }} minutes"
          exit 1

  undeploy:
    runs-on: ubuntu-latest
    # Run if:
    # 1. Deploy workflow completed successfully (automatic)
    # 2. Manual trigger with confirmation (manual)
    if: ${{ (github.event.workflow_run.conclusion == 'success') || (github.event_name == 'workflow_dispatch' && github.event.inputs.confirm_uninstall == 'true') }}

    steps:
      - name: Determine run type and delay
        id: run_info
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "run_type=automatic" >> $GITHUB_OUTPUT
            echo "🤖 Automatic undeploy triggered after successful deployment"
            echo "⏰ Will wait ${{ env.DELAY_MINUTES }} minutes before uninstalling from namespace: ${{ env.NAMESPACE }}"
          else
            echo "run_type=manual" >> $GITHUB_OUTPUT
            echo "👤 Manual undeploy triggered for namespace: ${{ env.NAMESPACE }}"
          fi

      - name: Wait before auto-uninstall
        if: steps.run_info.outputs.run_type == 'automatic'
        run: |
          echo "⏳ Waiting ${{ env.DELAY_MINUTES }} minutes before auto-uninstall..."
          echo "This gives time to test the deployment before cleanup."
          echo "To cancel this workflow, go to Actions tab and cancel this run."
          sleep ${{ env.DELAY_MINUTES }}m

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OpenShift CLI
        uses: redhat-actions/openshift-tools-installer@v1
        with:
          oc: 4

      - name: Login to OpenShift
        uses: redhat-actions/oc-login@v1
        with:
          openshift_server_url: ${{ secrets.OPENSHIFT_SERVER }}
          openshift_token: ${{ secrets.OPENSHIFT_TOKEN }}
          insecure_skip_tls_verify: true # Set to false if we have a valid certificate

      - name: Show deployment context
        run: |
          echo "📋 Undeploy Context:"
          echo "  Run Type: ${{ steps.run_info.outputs.run_type }}"
          echo "  Namespace: ${{ env.NAMESPACE }}"
          echo "  Trigger: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "  Triggered by: Deploy workflow completion"
            echo "  Deploy status: ${{ github.event.workflow_run.conclusion }}"
          fi

      - name: Show resources before uninstall
        run: |
          echo "📊 Resources in namespace ${{ env.NAMESPACE }} before uninstall:"
          make status NAMESPACE=${{ env.NAMESPACE }}

      - name: Uninstall metrics-summarizer with Makefile
        env:
          NAMESPACE: ${{ env.NAMESPACE }}
        run: |
          echo "🗑️ Uninstalling from namespace: ${{ env.NAMESPACE }}"
          make uninstall NAMESPACE=${{ env.NAMESPACE }}